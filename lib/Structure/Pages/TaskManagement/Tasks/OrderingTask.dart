import 'package:flutter/material.dart';

class OrderingTask extends StatelessWidget {
  const OrderingTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(
        child: Text("Not implemented"),
      ),
    );
  }
}